module.exports = {
  
  // compares saved gender with gender options
  checkGender: (a, b) => {
    console.log(a, b);
    const isEqual = a == b;
    return isEqual;
  },

  // compares saved category with category options
  checkCategory: (a, b) => {
    console.log(a, b);
    const isEqual = a == b;
    return isEqual;
  }
}